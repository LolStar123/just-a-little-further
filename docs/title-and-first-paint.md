# Title and first paint

Compared both proportions in the rendered 1280px layout: small ?just a little? with large italic ?further?, and oversized ?just? with small ?a little further?. Chose large ?further?: the modest effort leads into the larger horizon, while oversized ?just? distracts from the journey. Preserved the handwritten name, relocated above the title with plain ?presents?. UCL and profile links remain accessible in the corner.

The loader is now server-delivered HTML with critical inline visibility rules. Before any JavaScript arrives, the loader is visible and the rest of the page is hidden. Boot reuses that DOM, loads scene modules and fonts behind it, and reveals the scene only after readiness and three pushes. The existing drawn entrance follows. Checked with a two-second delay on boot.js, desktop/mobile, and three-click entry.
